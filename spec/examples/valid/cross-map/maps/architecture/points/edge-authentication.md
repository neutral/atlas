---
{
  "type": "point",
  "record": "anchor",
  "id": "edge-authentication",
  "posture": "asserted",
  "lifecycle": "active"
}
---

# Authenticate at the edge

Canonical context for authenticate at the edge.

This anchor record states a substantive project decision in its own right. It explains the governing boundary, the reason that the boundary exists, and the consequences that readers and software agents must preserve when they change related documents or implementation. The body is intentionally long enough to satisfy the deterministic anchor substance rule without relying on headings or link destinations.

## Connection: architecture-edge-authentication-area-boundary

This record affects Boundary because canonical context for authenticate at the edge.

## Connection: architecture-edge-authentication-area-security

This record affects Security because canonical context for authenticate at the edge.

## Connection: architecture-edge-authentication-supports-rotate-edge-keys

The boundary requires an operational key rotation practice.
