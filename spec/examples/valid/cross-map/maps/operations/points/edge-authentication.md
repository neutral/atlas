---
{
  "type": "point",
  "record": "context",
  "id": "edge-authentication"
}
---

# Operational context

Map-local context for edge-authentication.

Operations contributes the runtime consequences of the edge-authentication decision. Key material must rotate without removing the boundary, monitoring must distinguish rejected credentials from service failures, and incident procedures must preserve the canonical architecture decision.

## Connection: operations-edge-authentication-area-runtime

This record affects Runtime because map-local context for edge-authentication.

## Connection: operations-edge-authentication-area-security

This record affects Security because map-local context for edge-authentication.
