---
{
  "type": "point",
  "record": "anchor",
  "id": "rotate-edge-keys",
  "posture": "asserted",
  "lifecycle": "active"
}
---

# Rotate edge keys

Canonical context for rotate edge keys.

This anchor record states a substantive project decision in its own right. It explains the governing boundary, the reason that the boundary exists, and the consequences that readers and software agents must preserve when they change related documents or implementation. The body is intentionally long enough to satisfy the deterministic anchor substance rule without relying on headings or link destinations.

## Connection: operations-rotate-edge-keys-area-runtime

This record affects Runtime because canonical context for rotate edge keys.

## Connection: operations-rotate-edge-keys-area-security

This record affects Security because canonical context for rotate edge keys.

## Connection: operations-rotate-edge-keys-depends-on-edge-authentication

Rotation exists to preserve the edge boundary.
